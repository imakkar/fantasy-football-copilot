# Core data pipeline
nfl_data_py>=0.3.3
pandas>=1.5,<2.0        # nfl_data_py pins pandas<2.0 (needs Python 3.9-3.11)
numpy>=1.24,<2.0
PyYAML>=6.0            # reads configs/model_config.yaml

# Embeddings + vector store
sentence-transformers>=2.6.0    # local embedding backend (default, no API key)
chromadb>=0.5.0

# Generator LLM (Gemini is the free default; OpenAI is optional)
google-genai>=1.0.0
openai>=1.30.0

# Optional UI (built in Milestone 5)
streamlit>=1.35.0
